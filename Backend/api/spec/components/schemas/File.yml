type: object
properties:
  name:
    type: string
    description: Name of the file.
  size:
    type: integer
    description: Size of the file in bytes.
#  path:
#    type: string
#    description: Path where the file can be accessed.
  isDirectory:
    type: boolean
    description: Indicates whether the file is a directory.
required:
  - name
  - size
  - isDirectory
#  - path
